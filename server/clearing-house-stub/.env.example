# Example local-development environment for ds-clearing-house-stub.
#
# Usage:
#   cp .env.example .env
#   poetry run uvicorn app.main:app --reload --port 8084
#
# The stub holds state in memory only. Pod restart = empty CH.
# NOT for production use.

DS__PORT=8084
DS__HOST=0.0.0.0
DS__LOG_LEVEL=INFO
DS__ENVIRONMENT=development
