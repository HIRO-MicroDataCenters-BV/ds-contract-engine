"""ds-clearing-house — NextGen Data Space Contract Engine sub-service.

The per-node contract registry and tamper-evident audit ledger. Replaces
`clearing-house-stub` in deployed environments; the stub remains for local
compose and for the Generator's development-mode fallback.
"""

__version__ = "0.1.0"
