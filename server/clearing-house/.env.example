# ds-clearing-house — local development
#
# Storage settings arrive in step 2; this file currently covers only the
# service identity and logging.

DS__NODE_ID=localhost
DS__PORT=8085
DS__HOST=0.0.0.0
DS__LOG_LEVEL=INFO
DS__ENVIRONMENT=development
