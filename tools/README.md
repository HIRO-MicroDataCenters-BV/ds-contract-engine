# tools/

Helper scripts for the Contract Engine repo.

| Script | Purpose |
|---|---|
| [`generate-clients.sh`](generate-clients.sh) | Regenerate the Python client SDKs from `api/<service>/openapi.yaml`. |
